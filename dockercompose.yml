services:
  frontend:
    build:
      context: .
      dockerfile: dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://localhost:3000}
    container_name: enrichleads_frontend
    restart: unless-stopped
    ports:
      - "8080:80"
